#include <iostream>
#include <algorithm>
#include "windows.h"

using namespace std;

namespace WindowManagment {
    static auto new_window() {
        // WIN32::CreateThread(LPSECURITY_ATTRIBUTES());
    }
}
